let str = ["cat", "The fox", "Cow jumped over the moon", "good day"];

const strLength = function (stringArr) {
    for (i = 0; i <= 0; i++) {
        
        if (stringArr[i].length > i) {
            console.log("Item " + i + " in the list is the longest item");
        } 
        

        if (stringArr[i].length > i) {
            console.log("Item " + i + " in the list is the longest item");
        }
    }
}

console.log(strLength(str));

// Output: 21

// "Cow jumped over the moon" = 24

// "cat" = 3

// return 24 - 3

